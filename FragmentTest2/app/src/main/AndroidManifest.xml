<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fragmenttest2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FragmentTest2">
        <activity
            android:name=".monster.activity.MonsterActivity"
            android:exported="false" />
        <activity
            android:name=".dungeon.activity.DungeonActivity"
            android:exported="false" />
        <activity
            android:name=".SetImage$MainActivity"
            android:exported="false" />
        <activity
            android:name=".title.activity.TitleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>